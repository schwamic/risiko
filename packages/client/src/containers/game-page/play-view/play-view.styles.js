const wrapper = theme => ({
  display: 'flex',
  height: '100%',
  justifyContent: 'center',
  alignItems: 'center'
})

const mission = theme => ({
  maxWidth: theme.spacing(80)
})

export const styles = {
  mission,
  wrapper
}
