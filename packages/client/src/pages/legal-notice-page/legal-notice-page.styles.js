
const background = (theme) => ({
  display: 'flex',
  background: theme.palette.primary.main
})

const stack = {
  maxWidth: '650px',
  hyphens: 'auto'
}

export const styles = {
  background,
  stack
}
